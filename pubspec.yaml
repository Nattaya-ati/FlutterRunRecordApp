name: flutter_run_record_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"



dependencies:
  cupertino_icons: ^1.0.8
  ## dio ใช้ติดต่อserver  โดย Ctrl+ship+p   >Pubspec Assist:Add/update dependencies
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.8.0
  google_fonts: ^6.2.1



dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/
